name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
runs-on: 
ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build step
        run: echo "Building application"

  test:
    runs-on: ubuntu-latest
    steps:
      - name: Run tests
        run: echo "Running tests"

